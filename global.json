{
  "sdk": {
    "//": "Feature band pinned (10.0.1xx) with latestPatch: the same source builds the same way here, in the container image and in CI. Moving to a newer band is a deliberate change, together with the SDK tag of the Dockerfiles.",
    "rollForward": "latestPatch",
    "version": "10.0.100"
  },
  "msbuild-sdks": {
    "Aspire.AppHost.Sdk": "13.5.3"
  }
}
