# .NET
bin/
obj/
# Anclado a la raíz a propósito: sin la barra inicial el patrón vale a cualquier profundidad y, como en macOS
# git corre con core.ignorecase=true, también ignoraba Presentation/.../Contracts/Artifacts/. Dos archivos
# fuente vivieron solo en el disco y el CI llevaba cuatro corridas rojas sin que se notara en local.
/artifacts/
TestResults/
*.user
*.suo
.vs/
.vscode/
.idea/

# Config local y secretos — nunca al repo
appsettings.Local.json
appsettings.*.Local.json
*.env
.env*
!infra/.env.example
secrets.json
*.pfx

# Specs y contexto del proyecto — viven fuera de git
Spec/
Contexto*/

# SO
.DS_Store
Thumbs.db
