namespace SoftwareFactory.Api;

/// <summary>Marks the Api assembly for the integration-test host factory (both hosts have a top-level <c>Program</c>).</summary>
public sealed class ApiAssemblyMarker
{
    private ApiAssemblyMarker()
    {
    }
}
